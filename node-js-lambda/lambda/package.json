{
  "name": "node-js-lambda",
  "version": "1.0.0",
  "description": "Node.js Lambda with OpenTelemetry, API Gateway, and DynamoDB",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "@opentelemetry/api": "^1.9.1",
    "@trace0/lambda-otel-logger": "^1.0.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.0.0",
    "esbuild": "0.28.0",
    "typescript": "^5.7.0"
  }
}
